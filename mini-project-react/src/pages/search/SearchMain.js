import {useState} from "react";


const SearchMain = () => {
	// 이름을 통해 검색을 할 때 사용할 searchName
	// 값이 있으면 CarName 에 Like를 사용하여 값 추출
	// 값이 없으면 쿼리문에 더하지 않게 사용
	const [searchName, setSearchName] = useState("");
	
	const [price, setPrice] = useState("");
	
	
	
	
	
	
	
	
	return(
		<>
		
		</>
	)
}